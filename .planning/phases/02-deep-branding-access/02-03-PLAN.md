---
phase: 02-deep-branding-access
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: [apps/escardar_branding/css/login-overrides.css, apps/escardar_branding/css/landing-overrides.css]
autonomous: false
requirements: [BRAND-05, EXP-03]
must_haves:
  truths:
    - "Login page feels proprietary (no Nextcloud remnants, custom layout)"
    - "Landing page is visually aligned with the brand palette"
    - "Transition from Landing $\rightarrow$ Login is visually seamless"
  artifacts:
    - path: "apps/escardar_branding/css/login-overrides.css"
      provides: "Deep visual overrides for the login screen"
    - path: "apps/escardar_branding/css/landing-overrides.css"
      provides: "Styling for the proprietary landing page"
  key_links:
    - from: "escardar_branding app"
      to: "Nextcloud global styles"
      via: "CSS variable overrides"
---

<objective>
Apply high-fidelity visual polish to the entry flow, removing brand leakage and ensuring strict adherence to the Escardar Mail identity.

Purpose: Elevate the experience from "themed Nextcloud" to "proprietary mail service".
Output: Polished, brand-consistent landing and login pages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deep-branding-access/02-DISCOVERY.md
@.planning/phases/02-deep-branding-access/02-01-PLAN.md
@.planning/phases/02-deep-branding-access/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Login Screen Deep Polish</name>
  <files>apps/escardar_branding/css/login-overrides.css</files>
  <action>
    Create/Update CSS in the `escardar_branding` app to override the login screen:
    1. Remove any remaining "Nextcloud" text or logos from the footer/header of the login page.
    2. Adjust the login box positioning and styling (e.g., centered, custom border-radius, shadow).
    3. Apply the brand palette:
       - Primary button: `#2C363F` (Dark Grey/Blue)
       - Background accents: `#F8F7E5` (Cream)
    4. Ensure typography matches the professional brand identity.
  </action>
  <verify>
    <automated>grep -E "color: #2C363F" apps/escardar_branding/css/login-overrides.css</automated>
  </verify>
  <done>Login page CSS overrides are implemented and reference the brand palette.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Landing Page Visuals</name>
  <files>apps/escardar_branding/css/landing-overrides.css</files>
  <action>
    Apply professional styling to the `escardar_landing` app's template:
    1. Implement a modern, clean layout using the brand colors.
    2. Style the "Client Login" button to be a high-conversion call-to-action.
    3. Ensure responsiveness across mobile and desktop.
    4. Use CSS variables to maintain consistency with the overall branding app.
  </action>
  <verify>
    <automated>grep -E "background-color: #F8F7E5" apps/escardar_branding/css/landing-overrides.css</automated>
  </verify>
  <done>Landing page is visually polished and brand-consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final Visual Audit of Entry Flow</name>
  <files></files>
  <action>Verify the complete pre-auth flow: Landing Page $\rightarrow$ Login Page.</action>
  <verify>
    1. Visit the root domain. Verify the Landing Page looks professional and follows the brand guide.
    2. Click "Client Login". Verify the transition to the Login Page is seamless.
    3. Verify the Login Page displays the correct background, slogan, and has no Nextcloud branding.
    4. Check that colors (`#2C363F`, `#F8F7E5`) are used correctly.
  </verify>
  <done>User confirms the visual identity is correct and professional.</done>
  <what-built>The complete pre-auth flow: Landing Page $\rightarrow$ Login Page.</what-built>
  <how-to-verify>
    1. Visit the root domain. Verify the Landing Page looks professional and follows the brand guide.
    2. Click "Client Login". Verify the transition to the Login Page is seamless.
    3. Verify the Login Page displays the correct background, slogan, and has no Nextcloud branding.
    4. Check that colors (`#2C363F`, `#F8F7E5`) are used correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Client $\rightarrow$ UI (CSS injection)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | CSS Files | mitigate | Use standard Nextcloud app structure to ensure CSS is served from trusted paths. |
</threat_model>

<verification>
End-to-end visual audit of the entry flow.
</verification>

<success_criteria>
The user experience from landing to login is indistinguishable from a proprietary service.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-branding-access/02-03-SUMMARY.md`
</output>
