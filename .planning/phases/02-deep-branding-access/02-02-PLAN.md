---
phase: 02-deep-branding-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/escardar_landing/appinfo/info.xml, apps/escardar_landing/lib/Controller/LandingPageController.php, apps/escardar_landing/templates/landing.php]
autonomous: true
requirements: [EXP-03]
must_haves:
  truths:
    - "Users visiting the root URL see the Escardar Landing Page"
    - "Landing Page contains a 'Login' button that redirects to the login screen"
  artifacts:
    - path: "apps/escardar_landing"
      provides: "Custom landing page functionality"
  key_links:
    - from: "Landing Page"
      to: "/index.php/login"
      via: "Hyperlink/Redirect"
---

<objective>
Implement a proprietary pre-login landing page to separate the public brand experience from the application login.

Purpose: Create a professional "front door" for the service, enhancing the proprietary feel.
Output: A functional Nextcloud app that serves a branded landing page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deep-branding-access/02-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Escardar Landing App</name>
  <files>apps/escardar_landing/appinfo/info.xml</files>
  <action>
    Create the basic directory structure for the `escardar_landing` app.
    Create `appinfo/info.xml` with basic metadata (name, version, etc.).
    Ensure the app is in a state where it can be enabled via `occ app:enable`.
  </action>
  <verify>
    <automated>occ app:list | grep escardar_landing</automated>
  </verify>
  <done>App directory created and recognized by Nextcloud.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Public Landing Page Controller</name>
  <files>apps/escardar_landing/lib/Controller/LandingPageController.php, apps/escardar_landing/templates/landing.php</files>
  <action>
    1. Create `LandingPageController.php` extending `OCP\AppFramework\Controller`.
    2. Add a method with the `#[PublicPage]` attribute to serve the landing page.
    3. Create `templates/landing.php` with basic HTML structure, featuring:
       - The Escardar Mail brand header.
       - A value proposition section.
       - A prominent "Client Login" button linking to `/index.php/login`.
  </action>
  <verify>
    <automated>curl -s https://cloud.example.com/index.php/apps/escardar_landing/landing | grep "Client Login"</automated>
  </verify>
  <done>The public landing page is accessible and contains the login link.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Entry Point Redirect</name>
  <files></files>
  <action>
    Verify that the landing page can be accessed. 
    Note: In a production environment, this would involve a web server rewrite. 
    For this task, ensure the route is correctly registered and accessible via the app's public URL.
  </action>
  <verify>
    <automated>curl -I https://cloud.example.com/index.php/apps/escardar_landing/landing | grep "200 OK"</automated>
  </verify>
  <done>The landing page is successfully served as a public page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Landing Page $\rightarrow$ Login Page (Public to Auth boundary)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Landing Page | accept | Landing page is informational; no sensitive data handled. |
</threat_model>

<verification>
Access the landing page via browser, click "Client Login", and verify it leads to the login screen.
</verification>

<success_criteria>
A proprietary landing page exists and serves as the entry point to the login flow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-branding-access/02-02-SUMMARY.md`
</output>
