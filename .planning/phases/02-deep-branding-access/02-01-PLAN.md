---
phase: 02-deep-branding-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BRAND-05]
must_haves:
  truths:
    - "Login page displays the Escardar Mail slogan"
    - "Login page background image is set to the brand asset"
  artifacts:
    - path: "system config"
      provides: "Theming settings for slogan and background"
  key_links:
    - from: "occ theming:config"
      to: "Nextcloud database/config"
      via: "CLI"
---

<objective>
Establish the base visual identity of the login screen using Nextcloud's native theming engine.

Purpose: Provide the foundation for the branded login experience before applying deep CSS overrides.
Output: Configured Nextcloud instance with brand slogan and background image.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deep-branding-access/02-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Base Login Branding via OCC</name>
  <files></files>
  <action>
    Use the `occ` CLI to set the following theming parameters:
    1. Set the slogan to "Professional Mail for Professional Minds" (or current brand slogan).
    2. Set the background image to the designated brand background asset (ensure the file exists on the server first).
    
    Commands:
    - `occ theming:config slogan "Professional Mail for Professional Minds"`
    - `occ theming:config background /path/to/brand-background.jpg`
  </action>
  <verify>
    <automated>occ theming:config --list | grep -E "slogan|background"</automated>
  </verify>
  <done>Slogan and background path are correctly stored in the system configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Base Branding Appearance</name>
  <files></files>
  <action>
    Perform a request to the login page to verify the headers and content reflect the changes.
    Check that the background image is being served and the slogan text is present in the HTML.
  </action>
  <verify>
    <automated>curl -s https://cloud.example.com/index.php/login | grep -E "Professional Mail for Professional Minds"</automated>
  </verify>
  <done>Login page HTML contains the updated slogan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None - purely configuration changes.

## STRIDE Threat Register
No threats identified for base theming configuration.
</threat_model>

<verification>
Verify via browser that the login page visually reflects the slogan and background image.
</verification>

<success_criteria>
Nextcloud login page is no longer generic and displays the Escardar Mail slogan and background.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deep-branding-access/02-01-SUMMARY.md`
</output>
