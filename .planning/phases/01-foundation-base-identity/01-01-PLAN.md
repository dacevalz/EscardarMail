---
phase: 01-foundation-base-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BRAND-01, BRAND-02, BRAND-04]
user_setup: []

must_haves:
  truths:
    - "Header displays 'Escardar Mail'"
    - "Custom logo is visible in the header"
    - "Browser tab shows the Escardar favicon"
  artifacts:
    - path: "Nextcloud Config"
      provides: "Instance identity"
  key_links:
    - from: "occ config:system:set"
      to: "Nextcloud Database"
      via: "CLI command"
      pattern: "occ config:system:set"
---

<objective>
Configure the base identity of the Nextcloud instance, including the instance name, logo, and favicon.

Purpose: Establish the first visible touchpoints of the Escardar Mail brand.
Output: Updated Nextcloud system configuration and uploaded brand assets.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set Base Branding via OCC</name>
  <files>[]</files>
  <action>
    Use the `occ` CLI to configure the following:
    1. Set the instance name: `php occ config:system:set default_sitename --value="Escardar Mail"`
    2. Upload the custom logo: `php occ branding:logo` (or appropriate command to set the logo path).
    3. Upload the custom favicon: `php occ branding:favicon` (or appropriate command).
    
    Note: Use the provided asset 'Logo (7).png' and derive the favicon from it or use the corresponding asset if available in the root.
  </action>
  <verify>
    <automated>php occ config:system:get default_sitename | grep "Escardar Mail"</automated>
  </verify>
  <done>Instance name is set to 'Escardar Mail' and assets are uploaded to the system.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Base Identity</name>
  <files>[]</files>
  <what-built>Base identity configuration (Name, Logo, Favicon)</what-built>
  <how-to-verify>
    1. Navigate to the Nextcloud root URL.
    2. Verify the top-left header displays "Escardar Mail".
    3. Verify the custom logo is rendered correctly in the header.
    4. Verify the browser tab displays the custom Escardar favicon.
  </how-to-verify>
  <action>Verify visual assets in browser</action>
  <verify>Manual visual check</verify>
  <done>Identity assets confirmed as correct</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin CLI → System Config | Untrusted inputs to `occ` could modify system behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | `occ` CLI | mitigate | Run `occ` as the restricted web server user (www-data) |
</threat_model>

<verification>
- [ ] Instance name is "Escardar Mail"
- [ ] Logo is visible
- [ ] Favicon is visible
</verification>

<success_criteria>
The Nextcloud instance is now identified as "Escardar Mail" with the correct visual markers in the header and browser tab.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-base-identity/01-01-SUMMARY.md`
</output>
